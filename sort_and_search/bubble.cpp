#include <iostream>

class Bubble {
    private:

        swap(int *array, int a, int b) {
            int tmp = a;
            array[a] = b;
            array[b] = tmp;
        }

    public:

        bubbleSort(int *array, int n) {
            for(int i = n-1; i > 0; i--) {
                for(int j = 0; j < i; j++) {
                    
                }
            }
        }

}

int main() {
    return 0;
}
